## The usual picture is wrong

You already know the product: you type a message, a language model writes a reply. That is enough background.

The natural picture in your head is often this: _the model “understands” the question, forms the whole answer, then dumps a paragraph._ That picture is useful for using chat apps. It is a poor picture of how the reply is actually produced.

<MentalModelContrast />

What actually happens is narrower and more mechanical. The model repeatedly asks a smaller question:

> Given everything so far, what is a good **next small piece** of text?

Those pieces are **tokens**—sometimes whole words, sometimes fragments, sometimes punctuation. For this lesson, treat them as beads on a string. You do **not** need tokenization details.

## One run, one piece, then append

Engineers describe generation as a loop:

1. Look at the text so far (your prompt, then anything already written).
2. Run the model **once** to choose the next token.
3. **Append** that token to the text so far.
4. Repeat until the reply stops.

Each step conditions on a **growing past**. That process is called **autoregressive generation**—the name is optional; the loop is not.

The interactive below uses one **fixed** demo so you can re-watch the same path. It is schematic, not a live model.

<GenerationTimeline />

If you remember only one sentence from this lesson, make it this:

> The model did not write the reply all at once. It kept asking “what’s next?” and every answer became part of the past for the next ask.

## The past in play keeps growing

The same loop explains three everyday situations:

| What you do                         | What grows for the next token                   |
| ----------------------------------- | ----------------------------------------------- |
| You send a **longer prompt**        | More tokens exist before the first output token |
| The model writes a **longer reply** | Each new token joins the past for the next step |
| You continue a **multi-turn chat**  | Earlier turns usually stay in context           |

Nothing mystical: the loop always asks “what next, given so far?”—and “so far” is simply longer. That is why systems care about **sequence length** long before we talk about GPUs.

Compare three scenarios. Watch how tall the stack gets:

<ContextGrowthChart />

## Two meanings of “memory” (door to the rest of the course)

People use “memory” for two different things. Keep them separate:

<TwoMemories />

- **Lesson 2** asks how the model **looks back** along the past when choosing the next token.
- **Lesson 3** asks what the engine **stores** so looking back is affordable (the KV cache).

Today you only needed the loop and the growing stack of **text**.

## Check yourself

<Checkpoint>

- Can you describe generation as a loop, not a single “write the whole answer” step?
- If a reply doubles in length, what roughly doubles for the **next** token decision?
- In one sentence: how is “the chat remembers what I said” different from “the GPU stores a cache”?

</Checkpoint>

<details>
<summary>Assumptions & sources</summary>

**Assumptions**

- We ignore tokenizer details (BPE, special tokens).
- We ignore sampling (temperature, top-p)—those pick _which_ next token, not the loop shape.
- Demo tokens and prompt lengths are schematic and fixed for teaching.

**Sources**

- Standard decoder-only LM framing: next-token prediction under a growing context.
- Teaching spine: `docs/lesson-01-redesign.md` (vault-informed redesign).
- Outcomes: `docs/learning-outcomes.md` (Module 1).
- Deeper later: Attention (`S01`), KV cache notes in `docs/sources.md`.

</details>
