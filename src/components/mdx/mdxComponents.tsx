import {
  Assumptions,
  Checkpoint,
  Formula,
  Sources,
  TryThis,
} from "./LessonCallouts";

/** Components available inside lesson MDX files. */
export const mdxComponents = {
  Formula,
  Assumptions,
  Sources,
  TryThis,
  Checkpoint,
};
